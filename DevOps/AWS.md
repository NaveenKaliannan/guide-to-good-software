# AWS

#### Important Features in AWS
- **AWS IAM role** is a secure identity that provides permissions for AWS resources and can be assumed by a user, application, or service. One use case is that it grants permission to EC2 instance to read from an S3 bucket.
-  
